package AbstractConcepts;

public interface UpdatedRequirement extends CalciRequirement {

	//interface esxtending another interface
	
	//in java we can not have multiple extend but can have multoiple implementation
	//can not call two methods  from two class with extend keyword
	
}
